const assert = require('node:assert/strict')
const fs = require('node:fs')
const path = require('node:path')
const crypto = require('node:crypto')

const root = path.resolve(__dirname, '..')
const source = fs.readFileSync(path.join(root, 'shared/assessment/schema.js'), 'utf8')
const digest = crypto.createHash('sha256').update(source).digest('hex')
;[
  'utils/assessment-v2/generated/questionnaire-definitions.js',
  'cloudfunctions/datingProfile/assessment-v2-questionnaire-definitions.generated.js'
].forEach(file => assert.equal(fs.readFileSync(path.join(root, file), 'utf8'), source, file + ' is stale; run npm run sync:assessment'))
const manifest = JSON.parse(fs.readFileSync(path.join(root, 'shared/assessment/schema.manifest.json'), 'utf8'))
assert.equal(manifest.sha256, digest)
assert.equal(manifest.instrumentVersion, '2.1.0-pilot')
const scoringSource = fs.readFileSync(path.join(root, 'shared/assessment/scoring-rules.js'), 'utf8')
assert.equal(fs.readFileSync(path.join(root, 'utils/assessment-v2/generated/scoring-rules.js'), 'utf8'), scoringSource.replace("require('./schema')", "require('../questionnaire-definitions')"))
assert.equal(fs.readFileSync(path.join(root, 'cloudfunctions/datingProfile/assessment-v2-scoring-rules.generated.js'), 'utf8'), scoringSource.replace("require('./schema')", "require('./assessment-v2-questionnaire-definitions')"))
const reportRules = fs.readFileSync(path.join(root, 'shared/assessment/report-rules.js'), 'utf8')
assert.equal(fs.readFileSync(path.join(root, 'utils/assessment-v2/generated/report-rules.js'), 'utf8'), reportRules)
assert.equal(fs.readFileSync(path.join(root, 'cloudfunctions/datingProfile/assessment-v2-report-rules.generated.js'), 'utf8'), reportRules)
const reportEngine = fs.readFileSync(path.join(root, 'shared/assessment/report-engine.js'), 'utf8')
assert.equal(fs.readFileSync(path.join(root, 'utils/assessment-v2/generated/report-engine.js'), 'utf8'), reportEngine.replaceAll("require('./schema')", "require('./questionnaire-definitions')"))
assert.equal(fs.readFileSync(path.join(root, 'cloudfunctions/datingProfile/assessment-v2-report-engine.generated.js'), 'utf8'), reportEngine.replaceAll("require('./schema')", "require('./assessment-v2-questionnaire-definitions')").replaceAll("require('./scoring-rules')", "require('./assessment-v2-scoring-engine')").replaceAll("require('./report-rules')", "require('./assessment-v2-report-rules')"))
const interviewRules = fs.readFileSync(path.join(root, 'shared/assessment/interview-rules.js'), 'utf8')
assert.equal(fs.readFileSync(path.join(root, 'cloudfunctions/datingProfile/assessment-v2-interview-rules.generated.js'), 'utf8'), interviewRules.replace("const { HYPOTHESIS_RULE_VERSION } = require('./version')", "const HYPOTHESIS_RULE_VERSION = 'serious-match-interview-rules-1.0.0'"))
console.log('Assessment schema check OK:', digest)
